import { Banknote, CalendarDays, ChevronRight, CircleGauge, GraduationCap, Grid2X2, Home, Layers3, LoaderCircle, LogOut, Music2, Plus, RefreshCw, Search, Settings, Sparkles, UserPlus, Users, X } from 'lucide-react'
import { useMemo, useState, type FormEvent, type ReactNode } from 'react'
import { Navigate, NavLink, Route, Routes, useLocation, useNavigate } from 'react-router-dom'
import { productProfile, t } from '../lib/productProfile'
import { MusicDanceDataProvider, useMusicDanceData } from './MusicDanceDataProvider'
import { ProgramPage } from './ProgramPage'
import { FinancePage } from './FinancePage'
import { mdBransEkle, mdEgitmenEkle, mdGrubaKursiyerEkle, mdGrupEkle, mdKursiyerEkle, mdMekanEkle } from './service'
import type { MdUrunProfili } from './types'

function initials(value: string) { return value.split(/\s+/).filter(Boolean).slice(0, 2).map(x => x[0]).join('').toLocaleUpperCase('tr-TR') }

function Modal({ open, title, subtitle, onClose, children }: { open: boolean; title: string; subtitle?: string; onClose: () => void; children: ReactNode }) {
  if (!open) return null
  return <div className="md-modal-backdrop" onMouseDown={e => { if (e.target === e.currentTarget) onClose() }}>
    <section className="md-modal" role="dialog" aria-modal="true">
      <header><div><span>BS MÜZİK & DANS</span><h2>{title}</h2>{subtitle&&<p>{subtitle}</p>}</div><button type="button" onClick={onClose} aria-label="Kapat"><X size={18}/></button></header>
      <div className="md-modal-body">{children}</div>
    </section>
  </div>
}

function Login() {
  const { signIn, error } = useMusicDanceData()
  const [busy, setBusy] = useState(false)
  return <main className="md-login-page">
    <section className="md-login-art" aria-hidden="true"><div className="md-staff-lines"/><span className="md-art-orb orb-one"/><span className="md-art-orb orb-two"/></section>
    <section className="md-login-card">
      <div className="md-family-chip">BS EĞİTİM YÖNETİMİ · ÜRÜN AİLESİ</div>
      <img src="./bs-logo.svg" alt="BS" className="md-login-logo"/>
      <h1>{productProfile.brand}</h1>
      <p>Müzik ve dans kursunuzun kursiyer, eğitmen, grup, stüdyo ve finans operasyonlarını tek ritimde yönetin.</p>
      <div className="md-login-feature-row"><span><Music2/>Sanat odaklı</span><span><Grid2X2/>Tablet öncelikli</span><span><Sparkles/>Sade ve premium</span></div>
      <button className="md-primary large" disabled={busy} onClick={async()=>{setBusy(true);try{await signIn()}finally{setBusy(false)}}}>{busy?<LoaderCircle className="spin"/>:<ChevronRight/>}{busy?'Yönlendiriliyor…':'Google ile Giriş Yap'}</button>
      {error&&<div className="md-error">{error}</div>}
      <small className="md-login-foot">BS ürün ailesi · Güvenli kurum erişimi</small>
    </section>
  </main>
}

function Onboarding() {
  const { kurumOlustur, refreshing, error } = useMusicDanceData()
  const [name, setName] = useState('')
  const fallback = productProfile.key === 'muzik' || productProfile.key === 'dans' || productProfile.key === 'muzik-dans' ? productProfile.key : 'muzik-dans'
  const [profile, setProfile] = useState<MdUrunProfili>(fallback)
  return <main className="md-onboarding-page"><section className="md-onboarding-card">
    <div className="md-family-chip">İLK KURULUM</div><img src="./bs-logo.svg" alt="BS"/><h1>Sanat merkezinizi oluşturalım</h1><p>Bu işlem yalnız Müzik & Dans veri alanını oluşturur. Diğer BS uygulamalarındaki kayıtlar kullanılmaz.</p>
    <form onSubmit={async e=>{e.preventDefault();await kurumOlustur(name,profile)}}>
      <label>Kurum Adı<input required value={name} onChange={e=>setName(e.target.value)} placeholder="Örn. BS Sanat Akademisi"/></label>
      <label>Ürün Profili<select value={profile} onChange={e=>setProfile(e.target.value as MdUrunProfili)}><option value="muzik-dans">Müzik & Dans</option><option value="muzik">Müzik Kursu</option><option value="dans">Dans Kursu</option></select></label>
      <div className="md-onboarding-note"><Sparkles size={17}/><span>Merkez şube otomatik açılır. Branş, stüdyo ve eğitmenleri sonraki ekrandan ekleyebilirsiniz.</span></div>
      <button className="md-primary large" disabled={refreshing||!name.trim()}>{refreshing?<LoaderCircle className="spin"/>:<Plus/>}{refreshing?'Oluşturuluyor…':'Kurumu Oluştur'}</button>
    </form>{error&&<div className="md-error">{error}</div>}
  </section></main>
}

const nav = [
  {to:'/',label:'Ana Sayfa',icon:Home,end:true},
  {to:'/program',label:'Program',icon:CalendarDays},
  {to:'/kursiyerler',label:t.students,icon:Users},
  {to:'/egitmenler',label:t.teachers,icon:GraduationCap},
  {to:'/gruplar',label:'Gruplar',icon:Layers3},
  {to:'/finans',label:'Finans',icon:Banknote},
  {to:'/ayarlar',label:'Ayarlar',icon:Settings},
]

function Shell() {
  const { aktifKurum, kurumlar, kurumSec, refresh, refreshing, signOut } = useMusicDanceData()
  const location=useLocation()
  return <div className="md-shell">
    <aside className="md-sidebar">
      <div className="md-side-brand"><img src="./bs-logo.svg" alt="BS"/><div><strong>{productProfile.brandShort}</strong><span>{productProfile.brandSuffix}</span></div></div>
      <nav>{nav.map(({to,label,icon:Icon,end})=><NavLink key={to} to={to} end={end}><Icon size={18}/><span>{label}</span></NavLink>)}</nav>
      <div className="md-side-art"><span>BS</span><p>Sanatı yönetin,<br/>yazılımı değil.</p></div>
    </aside>
    <div className="md-main">
      <header className="md-topbar">
        <div className="md-topbar-title"><span>{location.pathname==='/'?'YÖNETİM ÖZETİ':nav.find(x=>x.to===location.pathname)?.label?.toLocaleUpperCase('tr-TR')||'BS MÜZİK & DANS'}</span><strong>{aktifKurum?.kurum_adi}</strong></div>
        <div className="md-topbar-actions">
          {kurumlar.length>1&&<select value={aktifKurum?.kurum_id||''} onChange={e=>kurumSec(e.target.value)}>{kurumlar.map(x=><option key={x.kurum_id} value={x.kurum_id}>{x.kurum_adi}</option>)}</select>}
          <button type="button" onClick={()=>void refresh()} aria-label="Yenile"><RefreshCw size={17} className={refreshing?'spin':''}/></button>
          <button type="button" onClick={()=>void signOut()} aria-label="Çıkış"><LogOut size={17}/></button>
        </div>
      </header>
      <main className="md-content"><Routes>
        <Route path="/" element={<Dashboard/>}/><Route path="/program" element={<Program/>}/><Route path="/kursiyerler" element={<Students/>}/><Route path="/egitmenler" element={<Teachers/>}/><Route path="/gruplar" element={<Groups/>}/><Route path="/finans" element={<Finance/>}/><Route path="/ayarlar" element={<SettingsPage/>}/><Route path="*" element={<Navigate to="/" replace/>}/>
      </Routes></main>
      <nav className="md-bottom-nav">{nav.filter(x=>['/','/program','/kursiyerler','/gruplar','/ayarlar'].includes(x.to)).map(({to,label,icon:Icon,end})=><NavLink key={to} to={to} end={end}><Icon size={19}/><span>{label==='Ana Sayfa'?'Ana':label}</span></NavLink>)}</nav>
    </div>
  </div>
}

function Dashboard(){
  const{data}=useMusicDanceData();const go=useNavigate();if(!data)return null
  const activeStudents=data.kursiyerler.filter(x=>x.durum!=='Pasif'),activeTeachers=data.egitmenler.filter(x=>x.durum!=='Pasif'),activeGroups=data.gruplar.filter(x=>x.durum!=='Pasif'),activeBranches=data.branslar.filter(x=>x.aktif!==false)
  const branchPulse=activeBranches.map(branch=>{const groups=activeGroups.filter(x=>x.brans_id===branch.brans_id);const memberIds=new Set(data.grupUyeleri.filter(x=>x.durum!=='Pasif'&&groups.some(g=>g.grup_id===x.grup_id)).map(x=>x.kursiyer_id));return{...branch,groups:groups.length,members:memberIds.size}}).sort((a,b)=>b.members-a.members||a.brans_adi.localeCompare(b.brans_adi,'tr'))
  return <div className="md-page-stack"><section className="md-hero"><div><span>BUGÜN · SANAT MERKEZİ NABZI</span><h1>İşleriniz tek ritimde.</h1><p>Kursiyer, eğitmen, grup ve stüdyo yapısını sakin bir yönetim yüzeyinden takip edin.</p></div><div className="md-hero-mark"><Music2/><span>BS</span></div></section>
    <section className="md-kpis"><button onClick={()=>go('/kursiyerler')}><span className="blue"><Users/></span><small>Aktif {t.student}</small><strong>{activeStudents.length}</strong><em>kayıt</em></button><button onClick={()=>go('/egitmenler')}><span className="violet"><GraduationCap/></span><small>Aktif {t.teacher}</small><strong>{activeTeachers.length}</strong><em>kadro</em></button><button onClick={()=>go('/gruplar')}><span className="rose"><Layers3/></span><small>Aktif Grup</small><strong>{activeGroups.length}</strong><em>topluluk</em></button><button onClick={()=>go('/ayarlar')}><span className="gold"><Grid2X2/></span><small>{t.room}</small><strong>{data.mekanlar.filter(x=>x.aktif!==false).length}</strong><em>mekan</em></button></section>
    <section className="md-panel"><div className="md-panel-heading"><div><span>SANAT HARİTASI</span><h2>Branş Nabzı</h2></div><button onClick={()=>go('/ayarlar')}>Branşları Yönet <ChevronRight size={15}/></button></div>{branchPulse.length?<div className="md-branch-grid">{branchPulse.slice(0,8).map((b,i)=><article key={b.brans_id} className={`tone-${i%4+1}`}><span className="md-branch-index">{String(i+1).padStart(2,'0')}</span><div><small>{b.kategori}</small><strong>{b.brans_adi}</strong><em>{b.groups} grup · {b.members} grup kursiyeri</em></div></article>)}</div>:<div className="md-empty"><Music2/><strong>Henüz branş yok.</strong><span>Ayarlar bölümünden piyano, gitar, bale veya kendi sanat alanınızı ekleyin.</span><button onClick={()=>go('/ayarlar')}>İlk Branşı Ekle</button></div>}</section>
    <section className="md-two-col"><div className="md-panel"><div className="md-panel-heading"><div><span>TOPLULUK</span><h2>Gruplar</h2></div><button onClick={()=>go('/gruplar')}>Tümü <ChevronRight size={15}/></button></div>{activeGroups.slice(0,4).map(g=>{const count=data.grupUyeleri.filter(x=>x.grup_id===g.grup_id&&x.durum!=='Pasif').length;return <div className="md-mini-row" key={g.grup_id}><span className="md-mini-icon"><Layers3/></span><div><strong>{g.grup_adi}</strong><small>{activeBranches.find(x=>x.brans_id===g.brans_id)?.brans_adi||'Branş yok'}</small></div><b>{count}{g.kapasite?` / ${g.kapasite}`:''}</b></div>})}{!activeGroups.length&&<div className="md-empty compact"><Layers3/><span>Henüz grup tanımlanmadı.</span></div>}</div><div className="md-panel"><div className="md-panel-heading"><div><span>MEKAN</span><h2>Stüdyo & Salonlar</h2></div><button onClick={()=>go('/ayarlar')}>Yönet <ChevronRight size={15}/></button></div>{data.mekanlar.filter(x=>x.aktif!==false).slice(0,4).map(m=><div className="md-mini-row" key={m.mekan_id}><span className="md-mini-icon gold"><Grid2X2/></span><div><strong>{m.mekan_adi}</strong><small>{m.mekan_turu}</small></div><b>{m.kapasite} kişi</b></div>)}{!data.mekanlar.length&&<div className="md-empty compact"><Grid2X2/><span>Henüz stüdyo veya salon eklenmedi.</span></div>}</div></section>
  </div>
}

function Students(){const{data,aktifKurum,refresh}=useMusicDanceData();const[q,setQ]=useState('');const[open,setOpen]=useState(false);const[busy,setBusy]=useState(false);if(!data||!aktifKurum)return null;const rows=data.kursiyerler.filter(x=>x.ad_soyad.toLocaleLowerCase('tr-TR').includes(q.toLocaleLowerCase('tr-TR')));return <div className="md-page-stack"><PageHead eyebrow="KURSİYER YÖNETİMİ" title={t.students} action={<button className="md-primary" onClick={()=>setOpen(true)}><UserPlus/> {t.student} Ekle</button>}/><div className="md-search"><Search/><input placeholder={`${t.student} ara…`} value={q} onChange={e=>setQ(e.target.value)}/><span>{rows.length} kayıt</span></div><div className="md-person-grid">{rows.map((x,i)=><article key={x.kursiyer_id} className={`md-person tone-${i%4+1}`}><div className="md-avatar">{initials(x.ad_soyad)}</div><div><strong>{x.ad_soyad}</strong><span>{x.seviye||'Seviye belirtilmedi'}</span><small>{x.telefon||x.email||'İletişim bilgisi yok'}</small></div><em>{x.durum}</em></article>)}</div>{!rows.length&&<Empty icon={<Users/>} text={`${t.student} bulunamadı.`}/>}<Modal open={open} title={`Yeni ${t.student}`} subtitle="Kısa profil bilgileri" onClose={()=>setOpen(false)}><form className="md-form" onSubmit={async e=>{e.preventDefault();setBusy(true);const f=new FormData(e.currentTarget);try{await mdKursiyerEkle(aktifKurum.kurum_id,{ad_soyad:String(f.get('ad_soyad')),telefon:String(f.get('telefon')||'')||null,email:String(f.get('email')||'')||null,seviye:String(f.get('seviye')||'')||null,dogum_tarihi:String(f.get('dogum_tarihi')||'')||null});await refresh();setOpen(false)}finally{setBusy(false)}}}><label>Ad Soyad<input name="ad_soyad" required autoFocus/></label><div className="md-form-two"><label>Telefon<input name="telefon" inputMode="tel"/></label><label>E-posta<input name="email" type="email"/></label></div><div className="md-form-two"><label>Seviye<input name="seviye" placeholder="Başlangıç / Orta / İleri"/></label><label>Doğum Tarihi<input name="dogum_tarihi" type="date"/></label></div><FormActions busy={busy} onCancel={()=>setOpen(false)} label={`${t.student} Kaydet`}/></form></Modal></div>}

function Teachers(){const{data,aktifKurum,refresh}=useMusicDanceData();const[q,setQ]=useState('');const[open,setOpen]=useState(false);const[busy,setBusy]=useState(false);if(!data||!aktifKurum)return null;const rows=data.egitmenler.filter(x=>x.ad_soyad.toLocaleLowerCase('tr-TR').includes(q.toLocaleLowerCase('tr-TR')));const branchNames=(id:string)=>data.egitmenBranslari.filter(x=>x.egitmen_id===id&&x.aktif!==false).map(x=>data.branslar.find(b=>b.brans_id===x.brans_id)?.brans_adi).filter(Boolean);return <div className="md-page-stack"><PageHead eyebrow="EĞİTİM KADROSU" title={t.teachers} action={<button className="md-primary" onClick={()=>setOpen(true)}><Plus/> {t.teacher} Ekle</button>}/><div className="md-search"><Search/><input placeholder={`${t.teacher} ara…`} value={q} onChange={e=>setQ(e.target.value)}/><span>{rows.length} kişi</span></div><div className="md-person-grid">{rows.map((x,i)=><article key={x.egitmen_id} className={`md-person teacher tone-${i%4+1}`}><div className="md-avatar">{initials(x.ad_soyad)}</div><div><strong>{x.ad_soyad}</strong><span>{branchNames(x.egitmen_id).join(' · ')||`${t.branch} tanımlanmamış`}</span><small>{x.telefon||x.email||'İletişim bilgisi yok'}</small></div><em>{x.durum}</em></article>)}</div>{!rows.length&&<Empty icon={<GraduationCap/>} text={`${t.teacher} bulunamadı.`}/>}<Modal open={open} title={`Yeni ${t.teacher}`} subtitle={`${t.branch} ve iletişim bilgileri`} onClose={()=>setOpen(false)}><form className="md-form" onSubmit={async e=>{e.preventDefault();setBusy(true);const f=new FormData(e.currentTarget);try{await mdEgitmenEkle(aktifKurum.kurum_id,{ad_soyad:String(f.get('ad_soyad')),telefon:String(f.get('telefon')||'')||null,email:String(f.get('email')||'')||null},f.getAll('brans_id').map(String));await refresh();setOpen(false)}finally{setBusy(false)}}}><label>Ad Soyad<input name="ad_soyad" required autoFocus/></label><div className="md-form-two"><label>Telefon<input name="telefon" inputMode="tel"/></label><label>E-posta<input name="email" type="email"/></label></div><fieldset><legend>{t.branch}</legend><div className="md-check-grid">{data.branslar.filter(x=>x.aktif!==false).map(x=><label key={x.brans_id}><input type="checkbox" name="brans_id" value={x.brans_id}/><span>{x.brans_adi}</span></label>)}</div></fieldset><FormActions busy={busy} onCancel={()=>setOpen(false)} label={`${t.teacher} Kaydet`}/></form></Modal></div>}

function Groups(){const{data,aktifKurum,refresh}=useMusicDanceData();const[open,setOpen]=useState(false);const[memberGroup,setMemberGroup]=useState<string|null>(null);const[busy,setBusy]=useState(false);if(!data||!aktifKurum)return null;const active=data.gruplar.filter(x=>x.durum!=='Pasif');return <div className="md-page-stack"><PageHead eyebrow="TOPLULUK DERSLERİ" title="Gruplar" action={<button className="md-primary" onClick={()=>setOpen(true)}><Plus/> Grup Oluştur</button>}/><div className="md-group-grid">{active.map((g,i)=>{const count=data.grupUyeleri.filter(x=>x.grup_id===g.grup_id&&x.durum!=='Pasif').length,capacity=Number(g.kapasite||0),pct=capacity?Math.min(100,Math.round(count/capacity*100)):0;return <article className={`md-group-card tone-${i%4+1}`} key={g.grup_id}><header><span className="md-group-symbol"><Layers3/></span><div><small>{data.branslar.find(x=>x.brans_id===g.brans_id)?.brans_adi||t.branch}</small><strong>{g.grup_adi}</strong></div><em>{g.durum}</em></header><div className="md-group-tags">{g.seviye&&<span>{g.seviye}</span>}{g.yas_grubu&&<span>{g.yas_grubu}</span>}</div><div className="md-occupancy"><div><span>Kontenjan</span><b>{count}{capacity?` / ${capacity}`:''}</b></div>{capacity>0&&<><div className="md-progress"><span style={{width:`${pct}%`}}/></div><small>%{pct} dolu</small></>}</div><div className="md-group-meta"><span><small>{t.teacher}</small><b>{data.egitmenler.find(x=>x.egitmen_id===g.varsayilan_egitmen_id)?.ad_soyad||'—'}</b></span><span><small>{t.room}</small><b>{data.mekanlar.find(x=>x.mekan_id===g.varsayilan_mekan_id)?.mekan_adi||'—'}</b></span></div><button className="md-secondary full" onClick={()=>setMemberGroup(g.grup_id)}><UserPlus/> Kursiyer Ekle</button></article>})}</div>{!active.length&&<Empty icon={<Layers3/>} text="Henüz grup oluşturulmadı."/>}<Modal open={open} title="Yeni Grup" subtitle="Branş, eğitmen, stüdyo ve kontenjan" onClose={()=>setOpen(false)}><form className="md-form" onSubmit={async e=>{e.preventDefault();setBusy(true);const f=new FormData(e.currentTarget);try{await mdGrupEkle(aktifKurum.kurum_id,{grup_adi:String(f.get('grup_adi')),brans_id:String(f.get('brans_id')||'')||null,varsayilan_egitmen_id:String(f.get('egitmen_id')||'')||null,varsayilan_mekan_id:String(f.get('mekan_id')||'')||null,kapasite:Number(f.get('kapasite')||0)||null,seviye:String(f.get('seviye')||'')||null,yas_grubu:String(f.get('yas_grubu')||'')||null});await refresh();setOpen(false)}finally{setBusy(false)}}}><label>Grup Adı<input name="grup_adi" required autoFocus placeholder="Örn. Bale Başlangıç A"/></label><div className="md-form-two"><label>{t.branch}<select name="brans_id" required><option value="">Seçin</option>{data.branslar.filter(x=>x.grup_uygun&&x.aktif!==false).map(x=><option key={x.brans_id} value={x.brans_id}>{x.brans_adi}</option>)}</select></label><label>{t.teacher}<select name="egitmen_id"><option value="">Seçin</option>{data.egitmenler.filter(x=>x.durum!=='Pasif').map(x=><option key={x.egitmen_id} value={x.egitmen_id}>{x.ad_soyad}</option>)}</select></label></div><div className="md-form-two"><label>{t.room}<select name="mekan_id"><option value="">Seçin</option>{data.mekanlar.filter(x=>x.aktif!==false).map(x=><option key={x.mekan_id} value={x.mekan_id}>{x.mekan_adi}</option>)}</select></label><label>Kontenjan<input name="kapasite" type="number" min="1"/></label></div><div className="md-form-two"><label>Seviye<input name="seviye"/></label><label>Yaş Grubu<input name="yas_grubu" placeholder="Örn. 8–12"/></label></div><FormActions busy={busy} onCancel={()=>setOpen(false)} label="Grubu Kaydet"/></form></Modal><Modal open={!!memberGroup} title="Gruba Kursiyer Ekle" subtitle={active.find(x=>x.grup_id===memberGroup)?.grup_adi} onClose={()=>setMemberGroup(null)}><form className="md-form" onSubmit={async e=>{e.preventDefault();if(!memberGroup)return;setBusy(true);const f=new FormData(e.currentTarget);try{await mdGrubaKursiyerEkle(aktifKurum.kurum_id,memberGroup,String(f.get('kursiyer_id')),Number(f.get('birim_ucret')||0)||null);await refresh();setMemberGroup(null)}finally{setBusy(false)}}}><label>{t.student}<select name="kursiyer_id" required><option value="">Seçin</option>{data.kursiyerler.filter(x=>x.durum!=='Pasif'&&!data.grupUyeleri.some(u=>u.grup_id===memberGroup&&u.kursiyer_id===x.kursiyer_id&&u.durum!=='Pasif')).map(x=><option key={x.kursiyer_id} value={x.kursiyer_id}>{x.ad_soyad}</option>)}</select></label><label>Birim Ücret <small>(opsiyonel)</small><input name="birim_ucret" type="number" min="0" step="0.01"/></label><FormActions busy={busy} onCancel={()=>setMemberGroup(null)} label="Gruba Ekle"/></form></Modal></div>}

function SettingsPage(){const{data,aktifKurum,refresh}=useMusicDanceData();const[branchOpen,setBranchOpen]=useState(false);const[venueOpen,setVenueOpen]=useState(false);const[busy,setBusy]=useState(false);if(!data||!aktifKurum)return null;return <div className="md-page-stack"><PageHead eyebrow="KURUM YAPISI" title="Ayarlar"/><section className="md-two-col"><div className="md-panel"><div className="md-panel-heading"><div><span>SANAT ALANLARI</span><h2>{t.branch}</h2></div><button onClick={()=>setBranchOpen(true)}><Plus/> Ekle</button></div><div className="md-tag-list">{data.branslar.map(x=><span key={x.brans_id}><b>{x.brans_adi}</b><small>{x.kategori} · {x.varsayilan_sure_dk} dk</small></span>)}</div>{!data.branslar.length&&<div className="md-empty compact"><Music2/><span>Branş eklenmedi.</span></div>}</div><div className="md-panel"><div className="md-panel-heading"><div><span>MEKANLAR</span><h2>{t.room}</h2></div><button onClick={()=>setVenueOpen(true)}><Plus/> Ekle</button></div>{data.mekanlar.map(x=><div className="md-mini-row" key={x.mekan_id}><span className="md-mini-icon gold"><Grid2X2/></span><div><strong>{x.mekan_adi}</strong><small>{x.mekan_turu}</small></div><b>{x.kapasite} kişi</b></div>)}{!data.mekanlar.length&&<div className="md-empty compact"><Grid2X2/><span>Mekan eklenmedi.</span></div>}</div></section><Modal open={branchOpen} title={`${t.branch} Ekle`} onClose={()=>setBranchOpen(false)}><form className="md-form" onSubmit={async e=>{e.preventDefault();setBusy(true);const f=new FormData(e.currentTarget);try{await mdBransEkle(aktifKurum.kurum_id,{brans_adi:String(f.get('brans_adi')),kategori:String(f.get('kategori')) as 'Müzik'|'Dans'|'Diğer',varsayilan_sure_dk:Number(f.get('sure')||60),bireysel_uygun:f.get('bireysel')==='on',grup_uygun:f.get('grup')==='on'});await refresh();setBranchOpen(false)}finally{setBusy(false)}}}><label>{t.branch} Adı<input name="brans_adi" required autoFocus placeholder="Piyano / Bale / Modern Dans"/></label><div className="md-form-two"><label>Kategori<select name="kategori" defaultValue={productProfile.key==='dans'?'Dans':'Müzik'}><option>Müzik</option><option>Dans</option><option>Diğer</option></select></label><label>Varsayılan Süre<input name="sure" type="number" min="15" max="360" defaultValue="60"/></label></div><div className="md-check-grid"><label><input type="checkbox" name="bireysel" defaultChecked/><span>Bireysel derse uygun</span></label><label><input type="checkbox" name="grup" defaultChecked/><span>Grup dersine uygun</span></label></div><FormActions busy={busy} onCancel={()=>setBranchOpen(false)} label="Branşı Kaydet"/></form></Modal><Modal open={venueOpen} title={`${t.room} Ekle`} onClose={()=>setVenueOpen(false)}><form className="md-form" onSubmit={async e=>{e.preventDefault();setBusy(true);const f=new FormData(e.currentTarget);try{await mdMekanEkle(aktifKurum.kurum_id,{mekan_adi:String(f.get('mekan_adi')),mekan_turu:String(f.get('mekan_turu')) as 'Stüdyo'|'Salon'|'Online'|'Diğer',kapasite:Number(f.get('kapasite')||1),sube_id:data.subeler[0]?.sube_id||null});await refresh();setVenueOpen(false)}finally{setBusy(false)}}}><label>Mekan Adı<input name="mekan_adi" required autoFocus placeholder="Stüdyo 1"/></label><div className="md-form-two"><label>Tür<select name="mekan_turu"><option>Stüdyo</option><option>Salon</option><option>Online</option><option>Diğer</option></select></label><label>Kapasite<input name="kapasite" type="number" min="1" defaultValue="1" required/></label></div><FormActions busy={busy} onCancel={()=>setVenueOpen(false)} label="Mekanı Kaydet"/></form></Modal></div>}

function Program(){return <ProgramPage/>}
function Finance(){return <FinancePage/>}
function ComingSoon({icon,eyebrow,title,text}:{icon:ReactNode;eyebrow:string;title:string;text:string}){return <div className="md-page-stack"><section className="md-coming"><span>{icon}</span><div><small>{eyebrow}</small><h1>{title}</h1><p>{text}</p><em>Altyapı aşaması · mevcut BS Eğitim finans/program motoruna dokunulmaz</em></div></section></div>}
function PageHead({eyebrow,title,action}:{eyebrow:string;title:string;action?:ReactNode}){return <section className="md-page-head"><div><span>{eyebrow}</span><h1>{title}</h1></div>{action}</section>}
function Empty({icon,text}:{icon:ReactNode;text:string}){return <div className="md-empty">{icon}<strong>{text}</strong><span>İlk kaydı sağ üstteki işlem düğmesinden ekleyebilirsiniz.</span></div>}
function FormActions({busy,onCancel,label}:{busy:boolean;onCancel:()=>void;label:string}){return <div className="md-form-actions"><button type="button" className="md-secondary" onClick={onCancel}>Vazgeç</button><button className="md-primary" disabled={busy}>{busy?<LoaderCircle className="spin"/>:<Plus/>}{busy?'Kaydediliyor…':label}</button></div>}

function Root(){const{session,kurumlar,aktifKurum,data,loading,error,refresh}=useMusicDanceData();if(loading)return <main className="md-boot"><img src="./bs-logo.svg" alt="BS"/><LoaderCircle className="spin"/><span>{productProfile.brandShort} hazırlanıyor…</span></main>;if(!session)return <Login/>;if(!kurumlar.length)return <Onboarding/>;if(!aktifKurum||!data)return <main className="md-boot"><img src="./bs-logo.svg" alt="BS"/>{error?<><strong>Veri açılamadı</strong><span>{error}</span><button className="md-primary" onClick={()=>void refresh()}>Tekrar Dene</button></>:<LoaderCircle className="spin"/>}</main>;return <Shell/>}

export function MusicDanceApp(){return <MusicDanceDataProvider><Root/></MusicDanceDataProvider>}
