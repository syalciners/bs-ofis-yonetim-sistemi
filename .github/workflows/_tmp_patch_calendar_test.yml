name: Geçici Takvim UX test patch

on:
  push:
    branches:
      - v1-sabit-program-gorunum-sadelestir

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: v1-sabit-program-gorunum-sadelestir
          fetch-depth: 0
      - name: Test patch uygula
        run: python3 scripts/_tmp_patch_calendar_test.py
      - name: Değişikliği kaydet
        run: |
          if git diff --quiet; then
            echo "Değişiklik yok."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add scripts/check-calendar-ux.mjs
          git commit -m "Takvim UX testini yeni Sabit Program görünümüne güncelle"
          git push origin HEAD:v1-sabit-program-gorunum-sadelestir
