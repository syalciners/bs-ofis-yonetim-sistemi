name: Demo Anonymous E2E Tek Sefer

on:
  push:
    branches: [demo-hazirlik]
    paths:
      - '.github/workflows/demo-e2e-once.yml'
      - 'scripts/test-demo-e2e.mjs'

permissions:
  contents: read

jobs:
  demo-e2e:
    runs-on: ubuntu-latest
    env:
      DEMO_SUPABASE_URL: https://pparlcdctnivmpiofuvz.supabase.co
      DEMO_SUPABASE_PUBLISHABLE_KEY: sb_publishable__IYAfCCcA6DWnpQ005xJdA__vvdiDGZ
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci --no-audit --no-fund
      - run: node scripts/test-demo-e2e.mjs
